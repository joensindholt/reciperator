/// <reference path="../typings/tsd.d.ts"/>

module app {
    'use strict';

    angular.module('app', []);
        // .config([], Config)
        // .run([], Run);

    function Config(): void {

    }

    function Run(): void {

    }
}
